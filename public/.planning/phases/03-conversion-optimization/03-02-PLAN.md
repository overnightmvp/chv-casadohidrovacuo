---
phase: 03-conversion-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - omvp-fmm-dashboard.html
autonomous: true

must_haves:
  truths:
    - "Every <a href=...> element in omvp-fmm-dashboard.html has at least one emoji character appearing before or within the link text"
    - "No link in omvp-fmm-dashboard.html has plain text without an emoji (e.g., 'View Landing Page' must become '🏠 View Landing Page')"
  artifacts:
    - path: "omvp-fmm-dashboard.html"
      provides: "Dashboard with consistent emoji prefix on every link for visual scannability"
---

<objective>
Emoji audit of omvp-fmm-dashboard.html. Every link must have an emoji. Read the full file, identify links missing emojis, add semantic emojis to each.

This runs in parallel with 03-01 (different file, no dependency).
</objective>

<execution_context>
Working directory: /Users/toshioj/Projects/dojjjo/fort-myers-movers/
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Full emoji audit of dashboard links</name>
  <files>omvp-fmm-dashboard.html</files>
  <action>
    READ /Users/toshioj/Projects/dojjjo/fort-myers-movers/omvp-fmm-dashboard.html in full.

    For every `<a href=...>` element, check if the link text starts with or contains an emoji. If it does NOT, add an appropriate emoji. Use semantic matching:

    **Operations / Doc links — semantic emoji guide:**
    - 90-minute execution / launch guide / quick start → ⚡
    - Launch checklist / checklist → ✅
    - KPI dashboard / metrics / analytics → 📊
    - Lead routing / routing setup → 🔄
    - Success metrics → 📈
    - Pricing guide → 💰
    - View landing page / site → 🏠
    - Blog hub / blog → 📝
    - New city boilerplate / onboarding → 🗺️
    - Contractor prospects / research → 🔍
    - Email templates → ✉️
    - Market intelligence / market intel → 🧭
    - SEO strategy → 📈 (or 🔍 if already used)
    - Roadmap / strategy → 🗺️
    - Brand system → 🎨
    - Privacy policy → 🔒
    - Index / redirect → ↗️
    - Any content page (cape-coral, naples, etc.) → 📍

    **Already have emojis (do NOT change):**
    - GitHub links (🐙)
    - Netlify links (🚀)
    - Formspree (📋)
    - Zapier (⚡)
    - Google Analytics (📊)
    - Facebook Ads (📣)
    - Thumbtack (⭐)
    - Any link already starting with an emoji

    Go through the ENTIRE file systematically. Check every single `<a` tag. Do not miss any.

    The file has approximately 53 links total. After your edits, all 53 should have emojis.
  </action>
  <verify>
    # Count links without emoji (should be 0)
    python3 -c "
import re
with open('/Users/toshioj/Projects/dojjjo/fort-myers-movers/omvp-fmm-dashboard.html', 'r') as f:
    content = f.read()
links = re.findall(r'<a[^>]+>([^<]+)</a>', content)
no_emoji = [l.strip() for l in links if l.strip() and not any(ord(c) > 127 for c in l)]
print(f'Links without emoji: {len(no_emoji)}')
for l in no_emoji[:10]:
    print(f'  - {l}')
" 2>/dev/null || grep -o '>>[^<]*<' /Users/toshioj/Projects/dojjjo/fort-myers-movers/omvp-fmm-dashboard.html | head -5
    echo "Total link count:" && grep -c "<a href" /Users/toshioj/Projects/dojjjo/fort-myers-movers/omvp-fmm-dashboard.html
  </verify>
  <done>All links in omvp-fmm-dashboard.html have emoji prefixes.</done>
</task>

</tasks>

<commit_rules>
After task completes:
1. git add omvp-fmm-dashboard.html
2. git commit -m "fix(03-02): add emoji to all dashboard links for consistent scannability"
3. Create .planning/phases/03-conversion-optimization/03-02-SUMMARY.md
4. git add and commit SUMMARY.md: "docs(03-02): dashboard emoji audit plan complete"
</commit_rules>

<output>
After completion, create 03-02-SUMMARY.md:
- File modified: omvp-fmm-dashboard.html
- Links audited: ~53 total
- Emojis added: N links that were missing them (document exact count)
- All links now have semantic emoji prefixes for visual scannability
</output>
